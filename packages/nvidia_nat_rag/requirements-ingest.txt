# Requirements for document ingestion (nv-ingest)
# Install in a SEPARATE virtual environment to avoid dependency conflicts
#
# Setup:
#   uv venv .venv-ingest
#   source .venv-ingest/bin/activate
#   uv pip install -r requirements-ingest.txt

nv-ingest==25.9.0
nv-ingest-api==25.9.0
nv-ingest-client==25.9.0
milvus-lite==2.4.12
pymilvus
openai>=1.51.0
requests
python-dotenv
markitdown
